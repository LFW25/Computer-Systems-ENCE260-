# include <stdio.h>
# include <stdint.h>
# include <stdbool.h>
# include <stdlib.h>
 
void printPrimesInRange(uint32_t n1, uint32_t n2)
{
    for (uint32_t i; i <= n2; i++) {
        if (n1 % 2 != 0) {
            for
        }
    }
}

int main(void)
{
    printPrimesInRange(2, 40);
}
